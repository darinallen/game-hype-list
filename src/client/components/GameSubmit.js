import React from 'react';

var GameSubmit = (props) => (
  <div className="game-submit">
    <input
      className="game-submit-form"
      type="text" />
    <button className="btn-submit">Submit</button>
  </div>
);

export default GameSubmit;
